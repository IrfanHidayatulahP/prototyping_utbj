<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <style>
        /* Sedikit kustomisasi scrollbar */
        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
    </style>
</head>
<body class="bg-gray-50 font-sans text-gray-800">

    <div class="flex h-screen overflow-hidden">
        
        <aside class="w-64 bg-white border-r border-gray-200 flex flex-col hidden md:flex z-10">
            <div class="h-16 flex items-center px-6 border-b border-gray-100">
                <div class="flex items-center gap-2 text-indigo-700 font-bold text-xl">
                    <i class="ph ph-graduation-cap text-2xl"></i>
                    <span>UTBJ Admin</span>
                </div>
            </div>

            <nav class="flex-1 px-3 py-6 space-y-1">
                <p class="px-3 text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">Menu Utama</p>
                
                <a href="/dashboard" class="flex items-center px-3 py-2 text-gray-600 hover:bg-gray-50 hover:text-indigo-600 rounded-lg transition-colors group">
                    <i class="ph ph-squares-four text-lg mr-3 text-gray-400 group-hover:text-indigo-600"></i>
                    <span class="font-medium">Dashboard</span>
                </a>

                <a href="/admin/kelola-gelombang" class="flex items-center px-3 py-2 bg-indigo-50 text-indigo-700 rounded-lg transition-colors group border border-indigo-100">
                    <i class="ph ph-waves text-lg mr-3 text-indigo-600"></i>
                    <span class="font-medium">Kelola Gelombang</span>
                </a>

                <a href="/admin/kelola-proposal" class="flex items-center px-3 py-2 text-gray-600 hover:bg-gray-50 hover:text-indigo-600 rounded-lg transition-colors group">
                    <i class="ph ph-files text-lg mr-3 text-gray-400 group-hover:text-indigo-600"></i>
                    <span class="font-medium">Data Proposal</span>
                </a>
                
                <a href="/admin/kelola-penelitian" class="flex items-center px-3 py-2 text-gray-600 hover:bg-gray-50 hover:text-indigo-600 rounded-lg transition-colors group">
                    <i class="ph ph-microscope text-lg mr-3 text-gray-400 group-hover:text-indigo-600"></i>
                    <span class="font-medium">Data Penelitian</span>
                </a>

                <a href="/admin/kelola-reviewer" class="flex items-center px-3 py-2 text-gray-600 hover:bg-gray-50 hover:text-indigo-600 rounded-lg transition-colors group">
                    <i class="ph ph-users text-lg mr-3 text-gray-400 group-hover:text-indigo-600"></i>
                    <span class="font-medium">Reviewer</span>
                </a>
            </nav>

            <div class="p-4 border-t border-gray-100">
                <a href="/logout" class="flex items-center gap-3 px-3 py-2 text-red-600 hover:bg-red-50 rounded-lg transition-colors text-sm font-medium">
                    <i class="ph ph-sign-out text-lg"></i>
                    Keluar Sistem
                </a>
            </div>
        </aside>

        <div class="flex-1 flex flex-col relative overflow-hidden">
            
            <header class="h-16 bg-white border-b border-gray-200 flex items-center justify-between px-4 sm:px-6 lg:px-8">
                <button class="md:hidden p-2 text-gray-500 hover:bg-gray-100 rounded-lg">
                    <i class="ph ph-list text-2xl"></i>
                </button>
                
                <h1 class="text-lg font-semibold text-gray-800 hidden sm:block">Manajemen Gelombang Hibah</h1>

                <div class="flex items-center gap-4">
                    <div class="text-right hidden sm:block">
                        <p class="text-sm font-medium text-gray-700"><%= user ? user.name : 'Administrator' %></p>
                        <p class="text-xs text-gray-500">Super Admin</p>
                    </div>
                    <div class="h-9 w-9 rounded-full bg-indigo-100 flex items-center justify-center text-indigo-600 font-bold border border-indigo-200 shadow-sm">
                        <i class="ph ph-user"></i>
                    </div>
                </div>
            </header>

            <main class="flex-1 overflow-y-auto bg-gray-50 p-4 sm:p-6 lg:p-8">
                
                <div class="bg-white border border-gray-200 rounded-xl shadow-sm">
                    
                    <div class="p-5 border-b border-gray-100 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                        
                        <div class="relative w-full sm:w-72">
                            <i class="ph ph-magnifying-glass absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"></i>
                            <input type="text" placeholder="Cari nama gelombang..." class="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all">
                        </div>

                        <a href="#" class="flex items-center justify-center gap-2 bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors shadow-sm">
                            <i class="ph ph-plus-circle text-lg"></i>
                            Buat Gelombang Baru
                        </a>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full text-left text-sm text-gray-600">
                            <thead class="bg-gray-50 text-gray-700 font-semibold border-b border-gray-200">
                                <tr>
                                    <th class="px-6 py-4 w-10 text-center">No</th>
                                    <th class="px-6 py-4">Nama Gelombang</th>
                                    <th class="px-6 py-4">Tahun Akademik</th>
                                    <th class="px-6 py-4">Periode</th>
                                    <th class="px-6 py-4 text-center">Status</th>
                                    <th class="px-6 py-4 text-right">Aksi</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-100">
                                
                                <% if (gelombangList && gelombangList.length > 0) { %>
                                    
                                    <% gelombangList.forEach((item, index) => { %>
                                    <tr class="hover:bg-gray-50 transition-colors group">
                                        <td class="px-6 py-4 text-center text-gray-400"><%= index + 1 %></td>
                                        
                                        <td class="px-6 py-4 font-medium text-gray-900">
                                            <%= item.nama_gelombang %>
                                        </td>
                                        
                                        <td class="px-6 py-4">
                                            <span class="bg-gray-100 text-gray-600 px-2 py-1 rounded text-xs font-medium">
                                                <%= item.tahun_akademik %>
                                            </span>
                                        </td>
                                        
                                        <td class="px-6 py-4 text-xs">
                                            <div class="flex flex-col gap-1">
                                                <span class="text-green-600 flex items-center gap-1"><i class="ph ph-calendar-check"></i> <%= item.tgl_buka %></span>
                                                <span class="text-red-500 flex items-center gap-1"><i class="ph ph-calendar-x"></i> <%= item.tgl_tutup %></span>
                                            </div>
                                        </td>
                                        
                                        <td class="px-6 py-4 text-center">
                                            <% if(item.status === 'Aktif') { %>
                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800 border border-green-200">
                                                    Aktif
                                                </span>
                                            <% } else { %>
                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-600 border border-gray-200">
                                                    Ditutup
                                                </span>
                                            <% } %>
                                        </td>
                                        
                                        <td class="px-6 py-4 text-right space-x-1">
                                            <button class="p-2 text-indigo-600 hover:bg-indigo-50 rounded-lg transition-colors" title="Edit">
                                                <i class="ph ph-pencil-simple text-lg"></i>
                                            </button>
                                            <button class="p-2 text-red-600 hover:bg-red-50 rounded-lg transition-colors" title="Hapus">
                                                <i class="ph ph-trash text-lg"></i>
                                            </button>
                                        </td>
                                    </tr>
                                    <% }) %>

                                <% } else { %>
                                    
                                    <tr>
                                        <td colspan="6" class="px-6 py-12 text-center">
                                            <div class="flex flex-col items-center justify-center text-gray-400">
                                                <i class="ph ph-files text-4xl mb-2"></i>
                                                <p class="text-sm">Belum ada data gelombang yang dibuat.</p>
                                                <p class="text-xs mt-1">Silakan klik tombol "Buat Gelombang Baru".</p>
                                            </div>
                                        </td>
                                    </tr>

                                <% } %>

                            </tbody>
                        </table>
                    </div>

                    <div class="px-6 py-4 border-t border-gray-100 flex items-center justify-between">
                        <p class="text-xs text-gray-500">
                            Menampilkan data 
                            <strong><%= gelombangList ? gelombangList.length : 0 %></strong> 
                            dari database.
                        </p>
                        <div class="flex gap-2">
                            <button class="px-3 py-1 border border-gray-200 rounded text-xs hover:bg-gray-50 text-gray-600 disabled:opacity-50" disabled>Sebelumnya</button>
                            <button class="px-3 py-1 border border-gray-200 rounded text-xs hover:bg-gray-50 text-gray-600 disabled:opacity-50" disabled>Selanjutnya</button>
                        </div>
                    </div>
                </div>

            </main>
        </div>
    </div>

</body>
</html>